# External connection-core consumer

This intentionally tiny project is configured against an installed
`aimee-core` package. CI builds it on Linux, macOS, and Windows so install/export
metadata and the portable connection API cannot silently depend on the Aimee
monorepo.
